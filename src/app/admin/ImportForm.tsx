'use client';
import {useFormState} from 'react-dom';
import {useState} from 'react';
import {importClubData} from '../../lib/club-import-actions';
export function ImportForm({initialCsv="",initialSource=""}:{initialCsv?:string;initialSource?:string}={}){const [state,action]=useFormState(importClubData,null);const [csv,setCsv]=useState(initialCsv),[fileError,setFileError]=useState('');return <section className="card space-y-4"><h2 className="display text-2xl">Flyt medlemmer og bookinger</h2><p className="text-sm text-slate">Eksportér fra det gamle system og tilpas kolonnerne til skabelonen. Importér højst 500 rækker ad gangen. Banenavne skal matche jeres baner præcist. Dette er en engangsimport; kalenderfeed er til løbende synkronisering.</p><pre className="overflow-x-auto rounded-xl bg-mist p-3 text-xs">{'type;email;name;phone;court;start;end;external_id\nmember;medlem@example.dk;Eksempel Navn;12345678;;;;\nbooking;medlem@example.dk;;;Bane 1;2027-01-10T18:00:00+01:00;2027-01-10T19:00:00+01:00;booking-123'}</pre><form action={action} className="space-y-3"><label className="label">Kildesystem<input className="input" name="source" defaultValue={initialSource} placeholder="Resasports" required/></label><label className="label block">Vælg CSV-fil<input type="file" accept=".csv,text/csv" className="input" onChange={async e=>{const file=e.target.files?.[0];if(!file)return;if(file.size>200000){setFileError('Filen må højst være 200 KB.');return;}setFileError('');setCsv(await file.text());}}/></label><label className="label">Eller indsæt CSV<textarea name="csv" className="input min-h-48" required value={csv} onChange={e=>setCsv(e.target.value)}/></label>{fileError&&<p role="alert">{fileError}</p>}<button name="intent" value="preview" className="btn-ghost">Vis forhåndsvisning</button>{state?.preview&&<div className="space-y-3"><p>{state.preview}</p><input type="hidden" name="digest" value={state.digest}/><label className="flex gap-2"><input name="confirmed" type="checkbox"/>Jeg har gennemgået data og har ret til at flytte dem.</label><button name="intent" value="import" className="btn-court">Bekræft import</button></div>}</form>{state?.error&&<p role="alert">{state.error}</p>}{state?.ok&&<p>{state.ok}</p>}</section>;}
